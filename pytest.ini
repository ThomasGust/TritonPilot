[pytest]
addopts = -q --basetemp=.pytest-tmp -m "not vision"
testpaths = tests
markers =
    hardware: tests that require physical ROV hardware
    integration: integration tests (no hardware required)
    vision: computer vision/image-video analysis tests skipped by default
